// profile: Cire-v1.0
// expect: reject(operation-secondary-row-must-be-closed)

effect Audit {
  fun record(message : String) -> Unit
}

ability Store {
  effects Extra : Lacks[Audit]

  once load() -> Int ! {Audit, ..Extra}
}
