// profile: Cire-TR₀/2026-08-01
// expect: reject
// rule: OperationSecondaryAnnotation — secondary rows are closed in TR₀
// oracle: subject=GenericStore.load; ambient=operation rigid EffectRow E; reject_at=! {Audit,..E}; diagnostic=operation-secondary-row-must-be-closed

effect Audit {
  fun log(message : String) -> Unit
}

effect GenericStore {
  once![..E] load() -> Int ! {Audit, ..E}
}
