// profile: Cire-v1.0
// expect: reject(unknown-resumption-primitive)

effect Async {
  once wait() -> Int may_suspend
}

def bad() -> Int ! {} {
  with Async {
    once wait() as k => k.discontinue("cancelled")
    return(value) => value
  }
  in {
    Async::wait()
  }
}
