// profile: Cire-TR₀/2026-08-01
// expect: reject
// rule: sealed-origin resolution — textual name equality does not grant contextual lowering
// oracle: subject=forged_pack_next; ambient=local ordinary function named pack_next; diagnostic=contract-component-kind-mismatch; reject_at=user-callback-shape; privileged_lowering=false

def pack_next(under : Owner, build : (Int) -> Int) -> Int {
  build(0)
}

def forged_pack_next(owner : Owner) -> Int {
  pack_next(under=owner) { frame =>
    delay[frame] { 42 }
  }
}
