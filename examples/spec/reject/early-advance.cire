// profile: Cire-TR₀/2026-08-01
// expect: reject
// rule: T-Advance — no matching clock lock
// oracle: subject=bad; ambient=FrameClock identity frame; reject_at=advance(value); diagnostic=no-matching-clock-lock; world=current(frame)

def![F : FrameClock] bad(
  frame : cap F,
  value : Next[frame, Int],
) -> Int {
  advance(value)
}
