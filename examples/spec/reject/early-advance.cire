// profile: Cire-v1.0
// expect: reject(no-matching-clock-lock)
// sealed prelude: FrameClock, Next, advance

def bad(
  frame : FrameClock,
  pending : Next[frame, Int],
) -> Int ! {} {
  advance(pending)
}
