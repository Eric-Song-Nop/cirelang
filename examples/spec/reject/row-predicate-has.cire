// profile: Cire-TR₀/2026-08-01
// expect: reject
// rule: only Lacks is a frozen explicit TR₀ row predicate
// oracle: subject=requires_log; ambient=rigid EffectRow E/Log effect; reject_at=Has[Log]; diagnostic=row-predicate-not-in-profile

def![..E : Has[Log]] requires_log(
  body : () -> Unit ! E,
) -> Unit ! E {
  body()
}
