// profile: Cire-TR₀/2026-08-01
// expect: reject
// rule: only Lacks is a frozen explicit TR₀ row predicate
// oracle: subject=ui_only; ambient=rigid EffectRow E/UIEffect effect; reject_at=Only[UIEffect]; diagnostic=row-predicate-not-in-profile

def![..E : Only[UIEffect]] ui_only(
  body : () -> Unit ! E,
) -> Unit ! E {
  body()
}
