// profile: Cire-TR₀/2026-08-01
// expect: reject
// rule: Surface grammar / CallArguments — positional arguments precede labels
// oracle: subject=bad; ambient=connect signature; reject_at=third-argument; diagnostic=positional-after-labelled; labels={secure}

def connect(host : String, port~ : Int, secure~ : Bool) -> Connection {
  loop {}
}

def bad() -> Connection {
  connect("example.test", secure=true, 443)
}
