// profile: Cire-TR₀/2026-08-01
// expect: accept
// rule: Lacks is the sole frozen explicit row predicate and discharges extension
// oracle: subject=add_log; ambient=rigid EffectRow E/Log effect with Lacks(E,Log); type=Unit; row={Log,..E}; obligation=Lacks(E,Log)

def![..E : Lacks[Log]] add_log(
  body : () -> Unit ! E,
) -> Unit ! {Log, ..E} {
  body()
}
