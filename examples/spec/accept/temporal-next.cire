// profile: Cire-v1.0
// expect: accept
// sealed prelude: FrameClock, Next, delay, advance

def next_value(
  frame : FrameClock,
  value : Int,
) -> Next[frame, Int] ! {} {
  delay[frame] { value + 1 }
}

def after_tick(
  frame : FrameClock,
  pending : Next[frame, Int],
) -> Int ! {frame} {
  frame.yield()
  advance(pending)
}
