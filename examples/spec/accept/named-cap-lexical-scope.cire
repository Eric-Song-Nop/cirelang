// profile: Cire-TR₀/2026-08-01
// expect: accept
// rule: T-Handle-Named-Paths — binder is live only inside the scoped computation
// oracle: subject=lexical_read; ambient=Reader ability/State handler; type=Int; row={}; identity_scope=scoped-computation; captures={}

ability Reader[A] {
  fun read() -> A
}

effect State[A] : Reader[A] {}

def lexical_read() -> Int {
  with handler State[Int] {
    fun read() => 42
  } as state
  in {
    state.read()
  }
}
